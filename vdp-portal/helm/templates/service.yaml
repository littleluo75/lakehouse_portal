apiVersion: v1
kind: Service
metadata:
  name: {{ include "vdp-portal.fullname" . }}
  labels:
    {{- include "vdp-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "vdp-portal.selectorLabels" . | nindent 4 }}
